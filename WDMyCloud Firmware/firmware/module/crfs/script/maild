case $1 in
 "stop")
	kill -9 `pidof mail_daemon` 2>NULL
	;;

  "start")
	mail_daemon &
	;;

  "restart")
	kill -9 `pidof mail_daemon` 2>NULL
	mail_daemon &
	;;
  *)
	echo "Argument Error"
	echo "Arguement must be \"stop\" \"start\" \"restart\""
	;;
esac
